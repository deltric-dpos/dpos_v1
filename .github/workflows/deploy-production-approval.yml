name: dpos-deploy-production

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      # Configure required reviewers in GitHub -> Settings -> Environments -> production
    steps:
      - name: Guardrail
        run: |
          echo "This workflow is intended to deploy a previously-built image tag to production."
          echo "Configure your Azure production resources + secrets, then implement the same update steps as staging."
          exit 1
